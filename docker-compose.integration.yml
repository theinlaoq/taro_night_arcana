services:
  tarot-backend:
    build:
      context: .
    image: taro-night-arcana:integration
    ports:
      - "8000:8000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
